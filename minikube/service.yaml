apiVersion: v1
kind: Service
metadata:
  name: guestbook-svc
  labels:
    app: guestbook
spec:
  type: NodePort
  ports:
  - port: 5000
    #targetPort: 5000
    nodePort: 30087
  selector:
    app: guestbook
